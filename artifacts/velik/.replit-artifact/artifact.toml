kind = "web"
previewPath = "/"
title = "Velik — Велосипеды нового поколения"
version = "1.0.0"
id = "artifacts/velik"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21174

[services.development]
run = "pnpm --filter @workspace/velik run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/velik", "run", "build" ]
publicDir = "artifacts/velik/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21174"
BASE_PATH = "/"
