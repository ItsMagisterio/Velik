{
  "version": 2,
  "buildCommand": "pnpm install && pnpm approve-builds && pnpm --filter velik run build:frontend",
  "outputDirectory": "velik/dist/public",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
